# Core runtime dependencies
# Covers: scraper, data cleaning, model training, prediction scripts
#
# xgboost-cpu is the CPU-only distribution of xgboost (same API, no CUDA libs).
# Users with an NVIDIA GPU who want GPU-accelerated training can swap this for
# `xgboost` and recompile.

pandas
numpy
scikit-learn
xgboost-cpu
joblib
requests
beautifulsoup4
